Dans le fichier htmlReader.py :

	modifier la méthode JSONParser pour qu'il envoit uniquement les noeuds
	contenant du texte, et les envoyer phrases par phrases à DBPedia

Dans le fichier main.py :
	
	modifier la méthode searchOnTheWeb pour qu'on puisse effectuer des 
	requetes de plusieurs mots

Dans le fichier jaccard2.py :
	
	modifier la méthode createGroups pour qu'il nomme avec un sens logique
	les groupes. Par exemple en se basant sur les URI.

	faire en sorte d'avoir des groupements cohérents. Genre les coefs de
	jaccard sont entre 0 et 0.1, du coup c'est dur d'avoir des groupements
	cohérents. Poser la question aux profs pour savoir comment faire
